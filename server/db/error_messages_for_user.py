master_repo_insert_error = "Repo ID and path must be unique in the system"


def reapeted_combinatio_master_backup_url(master, backup_url):
    return "There are already combinatation {" + master + ", " + backup_url + "}"
